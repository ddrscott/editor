{
  "name": "monaco-editor-spa",
  "version": "1.0.0",
  "description": "Monaco Editor SPA with collaborative code sharing",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply monaco-spaces"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "parcel": "^2.16.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.51.0"
  },
  "dependencies": {
    "monaco-editor": "^0.52.0",
    "yjs": "^13.6.0",
    "y-monaco": "^0.1.6"
  }
}
